length = float(input("Enter length of the rectangle: "))
width = float(input("Enter width of the rectangle: "))

area = length * width
print("Rectangle with  length {} and width {} has an area of {}".format(length, width, area))

